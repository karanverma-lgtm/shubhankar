Place these Lottie JSON exports here (source: LottieFiles.com, filter by free/CC license): ring-pop.json, heart-burst.json, confetti-burst.json, couple-hug.json, flip-clock.json (or build countdown with plain React state instead — simpler, see Prompt 13), car-travel.json, character-wave.json. Until real files are added, components fall back to a simple colored placeholder shape so the app doesn't crash.
